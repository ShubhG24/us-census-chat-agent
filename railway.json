{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "sleepApplication": false,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "sh -c \"(cd backend 2>/dev/null || true) && exec python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}\""
  }
}
